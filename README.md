# compare_zips.py

A tiny python script to make a task easier.

The script compares two zip files and determines the difference in what files each contains
or does not contain. It outputs 5 examples at the CLI (just to give a quick visual check).

## Usage

`python compare_zips.py zip1.zip zip2.zip`

